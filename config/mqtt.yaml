client_id: "${MQTT_CLIENT_ID}"
host: "${MQTT_HOST}"
port: ${MQTT_PORT}
keep_alive_secs: ${MQTT_KEEP_ALIVE_SECS}
username: "${MQTT_USERNAME}"  # Loaded from environment variable
password: "${MQTT_PASSWORD}"  # Loaded from environment variable
